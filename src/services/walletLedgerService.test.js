import test from"node:test";import assert from"node:assert/strict";import Wallet from"../models/Wallet.js";import{creditWallet,debitWallet,transferStars,safeWallet}from"./walletLedgerService.js";test("Wallet projection is non-negative integer Stars with versioning",()=>{assert.equal(Wallet.schema.path("balance").options.min,0);assert.equal(Wallet.schema.path("version").options.default,0);const invalid=new Wallet({user:"000000000000000000000001",balance:1.5});assert.ok(invalid.validateSync());assert.deepEqual(safeWallet({balance:5,version:2}),{balance:5,version:2,currency:"STARS"})});test("ledger service exposes only transaction-scoped mutation primitives",()=>{assert.equal(typeof creditWallet,"function");assert.equal(typeof debitWallet,"function");assert.equal(typeof transferStars,"function")});
