import WorldEntitlement from"../models/WorldEntitlement.js";import PremiumMembership from"../models/PremiumMembership.js";export async function publicationEntitlement(publication,viewer){if(!viewer?._id)return null;const creator=publication.creator?._id||publication.creator;if(viewer.role==="admin"||String(viewer._id)===String(creator))return null;if(publication.kind==="WORLD"){return await WorldEntitlement.exists({user:viewer._id,publication:publication._id,status:"ACTIVE"})?"ENTITLED_WORLD":null}if(publication.kind==="PREMIUM_WORLD"){return await PremiumMembership.exists({user:viewer._id,premiumPublication:publication._id,status:{$in:["ACTIVE","CANCEL_AT_PERIOD_END"]},currentPeriodEnd:{$gt:new Date()}})?"ACTIVE_PREMIUM_MEMBER":null}return null}
