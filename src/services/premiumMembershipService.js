import PremiumMembership from"../models/PremiumMembership.js";import Publication from"../models/Publication.js";import{executeFinancialCommand}from"./financialCommandService.js";import{transferStars,safeWallet}from"./walletLedgerService.js";import{fingerprint,idempotencyKey}from"../validators/financialValidator.js";import ApiError from"../utils/ApiError.js";import{FINANCIAL_ERROR_CODES}from"../constants/financialConstants.js";
const nextMonth=value=>{const d=new Date(value);d.setUTCMonth(d.getUTCMonth()+1);return d};const summary=m=>({id:m._id,publication:m.premiumPublication,creator:m.creator,status:m.status,starsPerPeriod:m.starsPerPeriod,currentPeriodStart:m.currentPeriodStart,currentPeriodEnd:m.currentPeriodEnd,cancelAtPeriodEnd:m.cancelAtPeriodEnd});
export async function joinPremium({user,publicationId,key}){key=idempotencyKey(key);return executeFinancialCommand({user:user._id,commandType:"JOIN_PREMIUM",idempotencyKey:key,requestFingerprint:fingerprint({publicationId:String(publicationId)})},async(session,command)=>{const publication=await Publication.findOne({_id:publicationId,kind:"PREMIUM_WORLD",status:"PUBLISHED"}).session(session);if(!publication?.publishedSnapshot)throw new ApiError(409,"Premium World is not joinable",FINANCIAL_ERROR_CODES.PUBLICATION_NOT_PURCHASABLE);if(String(publication.creator)===String(user._id))throw new ApiError(409,"Creators already have access to their own Premium World",FINANCIAL_ERROR_CODES.SELF_PURCHASE_NOT_REQUIRED);const activeMembershipKey=`${user._id}:${publication.creator}`;if(await PremiumMembership.exists({activeMembershipKey}).session(session))throw new ApiError(409,"Premium membership is already active",FINANCIAL_ERROR_CODES.MEMBERSHIP_ALREADY_ACTIVE);const price=publication.publishedSnapshot.metadata?.pricing?.starsAmount;if(![90,190,290].includes(price))throw new ApiError(409,"Premium price is invalid",FINANCIAL_ERROR_CODES.PUBLICATION_NOT_PURCHASABLE);const moved=await transferStars({fromUser:user._id,toUser:publication.creator,amount:price,debitType:"PREMIUM_JOIN_DEBIT",creditType:"PREMIUM_CREATOR_EARNING",referenceType:"PREMIUM_JOIN",referenceId:publication._id,publication:publication._id,creator:publication.creator,command,idempotencyKey:key,metadata:{priceSnapshotStars:price,periodUnit:"MONTH"}},session);const start=new Date(),end=nextMonth(start);const[membership]=await PremiumMembership.create([{user:user._id,creator:publication.creator,premiumPublication:publication._id,activeMembershipKey,status:"ACTIVE",starsPerPeriod:price,currentPeriodStart:start,currentPeriodEnd:end,cancelAtPeriodEnd:false,latestLedgerEntry:moved.debit.entry._id,idempotencyKey:key}],{session});return{resultReference:membership._id,membership:summary(membership),wallet:safeWallet(moved.debit.wallet),access:"ACTIVE_PREMIUM_MEMBER"}})}
export async function cancelPremium({user,membershipId,key}){key=idempotencyKey(key);return executeFinancialCommand({user:user._id,commandType:"CANCEL_PREMIUM",idempotencyKey:key,requestFingerprint:fingerprint({membershipId:String(membershipId)})},async(session)=>{const membership=await PremiumMembership.findOne({_id:membershipId,user:user._id}).session(session);if(!membership)throw new ApiError(404,"Membership not found","MEMBERSHIP_NOT_FOUND");if(["EXPIRED","CANCELED","REFUNDED"].includes(membership.status))throw new ApiError(409,"Membership is no longer active","MEMBERSHIP_EXPIRED");if(membership.cancelAtPeriodEnd)return{resultReference:membership._id,membership:summary(membership)};membership.cancelAtPeriodEnd=true;membership.status="CANCEL_AT_PERIOD_END";membership.canceledAt=new Date();membership.membershipVersion+=1;await membership.save({session});return{resultReference:membership._id,membership:summary(membership)}})}
export async function resumePremium({user,membershipId,key}){key=idempotencyKey(key);return executeFinancialCommand({user:user._id,commandType:"RESUME_PREMIUM",idempotencyKey:key,requestFingerprint:fingerprint({membershipId:String(membershipId)})},async(session)=>{const membership=await PremiumMembership.findOne({_id:membershipId,user:user._id,status:"CANCEL_AT_PERIOD_END",currentPeriodEnd:{$gt:new Date()}}).session(session);if(!membership)throw new ApiError(409,"Membership cannot be resumed","MEMBERSHIP_EXPIRED");membership.cancelAtPeriodEnd=false;membership.status="ACTIVE";membership.canceledAt=null;membership.membershipVersion+=1;await membership.save({session});return{resultReference:membership._id,membership:summary(membership)}})}
export{summary as safeMembership};
