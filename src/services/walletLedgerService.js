import Wallet from"../models/Wallet.js";import StarsLedgerEntry from"../models/StarsLedgerEntry.js";import ApiError from"../utils/ApiError.js";import{FINANCIAL_ERROR_CODES}from"../constants/financialConstants.js";
async function walletFor(user,session,{create=false}={}){const wallets=await Wallet.find({user}).limit(2).session(session);if(wallets.length>1)throw new ApiError(409,"Duplicate Wallet records require manual remediation","DUPLICATE_WALLETS");if(wallets[0]){const wallet=wallets[0];if(wallet.currency!=="STARS"||!wallet.ledgerActivatedAt||wallet.reconciliationStatus!=="MATCHED")throw new ApiError(409,"Wallet requires approved opening-balance migration","WALLET_REQUIRES_MIGRATION");return wallet}if(!create)throw new ApiError(422,"Wallet is unavailable",FINANCIAL_ERROR_CODES.WALLET_UNAVAILABLE);const[wallet]=await Wallet.create([{user,balance:0,currency:"STARS",version:0,ledgerActivatedAt:new Date(),reconciliationStatus:"MATCHED"}],{session});return wallet}
async function post({user,amount,direction,entryType,entryRole,referenceType,referenceId,publication,creator,counterpartyUser,command,idempotencyKey,reversalOf,metadata},session){const create=direction==="CREDIT";let wallet=await walletFor(user,session,{create});if(direction==="DEBIT"){wallet=await Wallet.findOneAndUpdate({_id:wallet._id,balance:{$gte:amount}},{$inc:{balance:-amount,version:1},$set:{currency:"STARS",ledgerActivatedAt:wallet.ledgerActivatedAt||new Date(),reconciliationStatus:"MATCHED"}},{new:true,session,runValidators:true});if(!wallet)throw new ApiError(422,"Insufficient Stars",FINANCIAL_ERROR_CODES.INSUFFICIENT_STARS)}else{wallet=await Wallet.findOneAndUpdate({_id:wallet._id},{$inc:{balance:amount,version:1},$set:{currency:"STARS",ledgerActivatedAt:wallet.ledgerActivatedAt||new Date(),reconciliationStatus:"MATCHED"}},{new:true,session,runValidators:true})}const[entry]=await StarsLedgerEntry.create([{accountUser:user,entryType,entryRole,direction,starsAmount:amount,signedAmount:direction==="CREDIT"?amount:-amount,balanceAfter:wallet.balance,referenceType,referenceId:String(referenceId),publication:publication||null,creator:creator||null,counterpartyUser:counterpartyUser||null,commandId:command._id,idempotencyKey,parentEntry:reversalOf||null,reversalOf:reversalOf||null,metadata}],{session});await Wallet.updateOne({_id:wallet._id},{$set:{lastLedgerEntry:entry._id}},{session});return{wallet,entry}}
export const creditWallet=(input,session)=>post({...input,direction:"CREDIT"},session);export const debitWallet=(input,session)=>post({...input,direction:"DEBIT"},session);
export async function transferStars({fromUser,toUser,amount,debitType,creditType,referenceType,referenceId,publication,creator,command,idempotencyKey,metadata},session){const debit=await debitWallet({user:fromUser,amount,entryType:debitType,entryRole:"FAN_DEBIT",referenceType,referenceId,publication,creator,counterpartyUser:toUser,command,idempotencyKey,metadata},session);const credit=await creditWallet({user:toUser,amount,entryType:creditType,entryRole:"CREATOR_EARNING",referenceType,referenceId,publication,creator,counterpartyUser:fromUser,command,idempotencyKey,metadata:{...metadata,settlementStatus:"UNSETTLED",grossStars:amount}},session);return{debit,credit}}
export const safeWallet=w=>({balance:w.balance,version:w.version,currency:"STARS"});
