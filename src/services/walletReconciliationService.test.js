import test from "node:test";import assert from"node:assert/strict";import{reconciliationReport}from"./walletReconciliationService.js";
test("reconciliation reports drift and unsafe record shapes without mutation",()=>{const report=reconciliationReport([{_id:"w1",user:"u1",balance:8,currency:"STARS"},{_id:"w2",user:"u1",balance:1.5,currency:"USD"}],[{_id:"e1",accountUser:"u1",signedAmount:5,entryType:"OPENING_BALANCE",commandId:"c1"},{_id:"e2",accountUser:"missing",signedAmount:2,entryType:"OPENING_BALANCE",commandId:"c2"}],[{_id:"c3",status:"SUCCEEDED",commandType:"ADMIN_CREDIT"}]);assert.equal(report.dryRun,true);assert.equal(report.duplicateWallets.length,1);assert.equal(report.drift.length,2);assert.deepEqual(report.fractionalBalances,["w2"]);assert.equal(report.unsupportedCurrency.length,1);assert.deepEqual(report.orphanedLedgerEntries,["e2"]);assert.deepEqual(report.succeededCommandsWithoutLedger,["c3"])});
