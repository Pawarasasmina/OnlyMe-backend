import test from"node:test";import assert from"node:assert/strict";import PremiumMembership from"../models/PremiumMembership.js";import{publicationAccess}from"./publicationAccessService.js";import{nextPremiumPeriod,PREMIUM_PERIOD_DAYS}from"./premiumMembershipService.js";test("Premium membership uses sparse unique active key",()=>assert.ok(PremiumMembership.schema.indexes().some(([keys,options])=>keys.activeMembershipKey===1&&options.unique&&options.sparse)));test("Premium remains preview-only without active server membership",()=>{const p={_id:"p",creator:"c",kind:"PREMIUM_WORLD",status:"PUBLISHED",publishedSnapshot:{}};assert.equal(publicationAccess(p,{_id:"fan"}),"PUBLIC_PREVIEW");assert.equal(publicationAccess(p,{_id:"fan"},{entitlement:"ACTIVE_PREMIUM_MEMBER"}),"ACTIVE_PREMIUM_MEMBER")});test("Premium renewal periods are exactly 30 days from purchase",()=>{const start=new Date("2026-01-31T12:00:00.000Z"),end=nextPremiumPeriod(start);assert.equal(PREMIUM_PERIOD_DAYS,30);assert.equal(end.toISOString(),"2026-03-02T12:00:00.000Z");assert.equal(end.getTime()-start.getTime(),30*24*60*60*1000)});
