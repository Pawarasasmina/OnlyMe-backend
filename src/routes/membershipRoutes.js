import{Router}from"express";import{join,cancel,resume}from"../controllers/membershipController.js";import{protect}from"../middleware/authMiddleware.js";import{financialMutationLimit}from"../middleware/financialRateLimit.js";const premium=Router();premium.post("/:publicationId/join",protect,financialMutationLimit,join);export const premiumWorldRoutes=premium;const memberships=Router();memberships.post("/:id/cancel",protect,financialMutationLimit,cancel);memberships.post("/:id/resume",protect,financialMutationLimit,resume);export default memberships;
