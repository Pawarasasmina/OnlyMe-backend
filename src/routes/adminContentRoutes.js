import { Router } from "express"; import { approveContent, getModerationContent, getModerationHistory, listModerationContent, rejectContent, requestContentChanges } from "../controllers/adminContentController.js"; import { protect } from "../middleware/authMiddleware.js"; import { authorize } from "../middleware/roleMiddleware.js";
const router = Router(); router.use(protect, authorize("admin")); router.get("/", listModerationContent); router.get("/:id", getModerationContent); router.get("/:id/history", getModerationHistory); router.post("/:id/approve", approveContent); router.post("/:id/request-changes", requestContentChanges); router.post("/:id/reject", rejectContent); export default router;
