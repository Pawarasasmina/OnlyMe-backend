import{Router}from"express";import{convertIncomeToCoins,getWallet,getLedger,getWorldEntitlements,getMemberships,topUpWallet}from"../controllers/walletController.js";import{protect}from"../middleware/authMiddleware.js";const router=Router();router.use(protect);router.get("/wallet",getWallet);router.post("/wallet/top-up",topUpWallet);router.post("/wallet/convert-income",convertIncomeToCoins);router.get("/wallet/ledger",getLedger);router.get("/entitlements/worlds",getWorldEntitlements);router.get("/memberships",getMemberships);export default router;
