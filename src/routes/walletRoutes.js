import{Router}from"express";import{getWallet,getLedger,getWorldEntitlements,getMemberships}from"../controllers/walletController.js";import{protect}from"../middleware/authMiddleware.js";const router=Router();router.use(protect);router.get("/wallet",getWallet);router.get("/wallet/ledger",getLedger);router.get("/entitlements/worlds",getWorldEntitlements);router.get("/memberships",getMemberships);export default router;
