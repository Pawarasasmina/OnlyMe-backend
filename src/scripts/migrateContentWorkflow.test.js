import assert from "node:assert/strict"; import test from "node:test"; import { inspectLegacyContent, migrationUpdate } from "./migrateContentWorkflow.js";
test("migration preserves legacy URLs and published state", () => { const item = { _id: "id", creator: "creator", title: "Short", topic: "Longer legacy title", status: "published", contentType: "image", accessLevel: "subscribers", images: [{ publicId: "p", url: "https://u", format: "jpg", isMain: true }] }; const update = migrationUpdate(item); assert.equal(update.status, "PUBLISHED"); assert.equal(update.title, "Longer legacy title"); assert.equal(update.media[0].secureUrl, "https://u"); assert.equal(update.legacyMigration.legacyPublished, true); });
test("migration rerun is idempotent and reports leak candidates", () => { assert.equal(migrationUpdate({ legacyMigration: { version: 1 } }), null); assert.equal(inspectLegacyContent({ creator: "c", status: "published", contentType: "image", accessLevel: "subscribers", images: [{ isMain: true }] }).publicLeakCandidate, true); });
